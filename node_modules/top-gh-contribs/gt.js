var ninetyDaysAgo = Date.now() - (1000 * 60 * 60 * 24 * 90),
    getTopContribs = require('./index');

getTopContribs({
    user: 'tryghost',
    repo: 'ghost',
    oauthKey: '7618cb77e67f5a75050d1abc2f9bedc100e83ed6',
    releaseDate: ninetyDaysAgo,
    count: 20,
    retry: true
}).then(function (r) {
    console.log('debug: ' + JSON.stringify(r));
    console.log('debug count: ' + r.length);
}).catch(function (err) {
  console.log(err.stack);
  console.log(JSON.stringify(err));
});
